export type Auth = {
  username: string;
  password: string;
};

/*
Represents the data required for authentication requests (e.g., signup, login).
Contains the plain text password provided by the user during authentication.
Used for handling request bodies in authentication endpoints.
*/
